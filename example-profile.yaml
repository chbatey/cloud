keyspaces:
  - name: test
    definition: "CREATE KEYSPACE IF NOT EXISTS test WITH replication = {'class': 'SimpleStrategy', 'replication_factor': '1'}"

tables:
  - name: kv
    definition: "CREATE TABLE IF NOT EXISTS kv (key TEXT PRIMARY KEY, value TEXT)"
    keyspace: test

statements:
#  - name: insert1
#    text: "insert INTO kv (key , value ) values (?, ?)"
#    variables:
#
#      - type: text
#        population: single
#        definition:
#          value: "three"
#
#      - type: text
#        population: single
#        definition:
#          value: "four"

  - name: insert2
    text: "insert INTO kv (key, value) values (?, ?)"
    variables:

      - type: text
        population: fixed
        definition:
          number: 100

      - type: text
        population: single
        definition:
          value: "four"


#  - name: query1
#    text: "select * from kv where key = ?"
#    variables:
#      - type: text
#        population: single
#        definition:
#          value: "three"

scenario:
  insert1: 10
  query1: 1


